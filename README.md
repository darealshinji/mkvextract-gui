Simple FLTK based mkvextract GUI for Linux.
Requires mkvinfo and mkvextract to be in PATH.

To compile with libfltk run `./build_fltk.sh && make`.
To compile using system-wide libfltk run `SYSTEM_FLTK=1 make`.

You can create a .mkv file for testing with `./test/mux.sh`

